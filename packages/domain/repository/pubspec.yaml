name: repository
description: Repository interfaces package
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.7.0
resolution: workspace

dependencies:
  model:
    path: ../model

dev_dependencies:
  lints: ^5.1.1
  very_good_analysis: ^7.0.0 